import {Request, response, Response} from "express";
import dotenv from "dotenv";
import { request } from "http";


dotenv.config();
export class middleware {
@param req
@param res
}

public async authenticatedToken(
    req: Request,
    res: Response
)